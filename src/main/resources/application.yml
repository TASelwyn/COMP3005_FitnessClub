spring:
  application:
    name: fitnessclub

  datasource:
    url: jdbc:postgresql://localhost:8652/fitnessclub
    username: comp3005
    password: password

  jpa:
    hibernate:
      ddl-auto: update # Set to "validate" for submission (requires generating liquibase changelog!!!)
    show-sql: true

  security:
    user:
      name: user
      password: user
server:
  port: 8080
